name: Apply report update

on:
  push:
    paths:
      - 'scripts/apply_ga4.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-report:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Apply GA4 module
        run: python scripts/apply_ga4.py

      - name: Commit report update
        run: |
          if git diff --quiet; then
            echo "No report changes to commit"
            exit 0
          fi
          git config user.name "hyrox-report-bot"
          git config user.email "actions@users.noreply.github.com"
          git add index.html
          git commit -m "Add GA4 web performance module"
          git push
